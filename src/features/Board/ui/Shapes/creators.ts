import { type ArrowShape, type CardShape, type LineShape, type RectangleShape } from '../Shape'

export const createLine = (args: LineShape): LineShape => {
  return args
}

export const createRectangle = (args: RectangleShape): RectangleShape => {
  return args
}

export const createArrow = (args: ArrowShape): ArrowShape => {
  return args
}

export const createCard = (args: CardShape): CardShape => {
  return args
}
